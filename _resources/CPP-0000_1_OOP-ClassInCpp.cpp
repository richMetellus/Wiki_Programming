class person
{
    char name[20];
    int socialSecurityNum;

public:
    void getdetails() {}
};

int main()
{
    // Object take up space in memory and have an associated address like a record in pascal or structure or union in C.
    // p1 is a object
    // in C++ you don't have to call the constructor with the ``new`` keyword directly lik Java
    person p1; 
}
